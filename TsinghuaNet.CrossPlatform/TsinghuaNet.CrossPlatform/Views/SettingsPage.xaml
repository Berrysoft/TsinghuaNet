<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TsinghuaNet.CrossPlatform.ViewModels"
             x:Class="TsinghuaNet.CrossPlatform.Views.SettingsPage"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:SettingsViewModel/>
    </ContentPage.BindingContext>
    <StackLayout Orientation="Vertical" Padding="16">
        <StackLayout Orientation="Horizontal">
            <Label VerticalOptions="Center" Style="{DynamicResource BodyStyle}">当前用户：</Label>
            <Label VerticalOptions="Center" Style="{DynamicResource BodyStyle}" Text="{Binding Credential.Username}"/>
            <Button Clicked="ChangeCredential" WidthRequest="80" Text="更改"/>
        </StackLayout>
    </StackLayout>
</ContentPage>