<StackPanel
    x:Class="TsinghuaNetUWP.SsidSuggestion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:TsinghuaNetUWP"
    xmlns:helper="using:TsinghuaNetHelper"
    mc:Ignorable="d" Orientation="Horizontal">
    <StackPanel.Resources>
        <DataTemplate x:Key="NetStateTemplate" x:DataType="local:NetStateBox">
            <TextBlock Text="{x:Bind helper:UserHelper.GetNetStateString(Value)}"/>
        </DataTemplate>
    </StackPanel.Resources>
    <TextBlock Width="120" VerticalAlignment="Center" Style="{x:Bind SsidStyle,Mode=OneWay}" Text="{x:Bind Ssid,Mode=OneWay}"/>
    <ComboBox Width="280" ItemsSource="{x:Bind States}" SelectedIndex="{x:Bind ValueToIndex(Value),BindBack=IndexToValue,Mode=TwoWay}" ItemTemplate="{StaticResource NetStateTemplate}"/>
</StackPanel>
