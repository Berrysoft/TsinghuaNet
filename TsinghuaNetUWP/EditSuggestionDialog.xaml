<ContentDialog
    x:Class="TsinghuaNetUWP.EditSuggestionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TsinghuaNetUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Background="{ThemeResource SystemControlAcrylicWindowMediumHighBrush}" CloseButtonStyle="{ThemeResource ButtonRevealStyle}"
    Title="编辑建议" DefaultButton="Primary" PrimaryButtonText="确定" CloseButtonText="取消">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Margin="10" Style="{ThemeResource BaseTextBlockStyle}" VerticalAlignment="Center">有线网</TextBlock>
        <ComboBox Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Margin="10" Style="{ThemeResource BaseTextBlockStyle}" VerticalAlignment="Center">移动流量</TextBlock>
        <ComboBox Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch"/>
        <TextBlock Grid.Column="0" Grid.Row="2" Margin="10" Style="{ThemeResource BaseTextBlockStyle}" VerticalAlignment="Center">无线网</TextBlock>
        <ListView Grid.ColumnSpan="2" Grid.Row="3" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:WlanSuggestionModel">
                    <StackPanel Orientation="Horizontal">
                        <TextBox Width="100" Text="{Binding Ssid}"/>
                        <ComboBox Width="200" SelectedValue="{Binding State}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
